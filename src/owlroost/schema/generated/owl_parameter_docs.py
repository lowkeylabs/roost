# src/owlroost/schema/generated/owl_parameter_docs.py
#
# Copyright (c) 2026 John Leonard
# SPDX-License-Identifier: GPL-3.0-or-later
# See LICENSE file in repository root.

"""
AUTO-GENERATED FILE.

Generated by:
    owlroost.tools.generate_explain_metadata

DO NOT EDIT MANUALLY.
"""

from __future__ import annotations

OWL_PARAMETER_DOCS = {
    "HFP_file_name": {
        "section": "household_financial_profile",
        "type": "string",
        "description": 'Filename of the HFP workbook (typically .xlsx). Resolved relative to the directory of the case TOML when loading. Use "None" if the case has no HFP (wages and contributions are then zero unless set another way).',
        "units": None,
        "notes": None,
    },
    "absTol": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Absolute convergence tolerance for the self-consistent loop objective.",
        "units": None,
        "notes": None,
    },
    "aca_start_year": {
        "section": "aca_settings",
        "type": "int",
        "description": "*(Optional)* Calendar year when ACA coverage begins (e.g. year of retirement). Years before this are treated as employer-covered — ACA cost is zero. Default 0 = ACA applies from the first year of the plan.",
        "units": "years",
        "notes": None,
    },
    "amoConstraints": {
        "section": "solver_options",
        "type": "boolean",
        "description": "*(Advanced)* Whether to use at-most-one (AMO) constraints in the optimization.",
        "units": None,
        "notes": None,
    },
    "amoRoth": {
        "section": "solver_options",
        "type": "boolean",
        "description": "*(Advanced)* Whether to enforce at-most-one (AMO) constraints preventing simultaneous Roth conversions and tax-free withdrawals.",
        "units": None,
        "notes": None,
    },
    "amoSurplus": {
        "section": "solver_options",
        "type": "boolean",
        "description": "*(Advanced)* Whether to enforce XOR constraints preventing simultaneous surplus deposits and withdrawals from taxable or tax-free accounts.",
        "units": None,
        "notes": None,
    },
    "bendersMaxIter": {
        "section": "solver_options",
        "type": "integer",
        "description": '*(Advanced)* Maximum number of Benders iterations when withDecomposition = "benders". Each iteration solves a subproblem LP (for the dual cut) plus a subproblem MIP (for the upper bound) plus a master MIP (for the new lower bound). In practice, convergence typically occurs within 1–3 iterations because the LP relaxation\'s bracket assignment is nearly globally optimal.',
        "units": None,
        "notes": None,
    },
    "beneficiary_fractions": {
        "section": "savings_assets",
        "type": "list of 3 or 4 floats",
        "description": "*(Married only)* Fraction of each account type (taxable, tax-deferred, tax-free, and optionally HSA) bequeathed to the surviving spouse. Each value should be between 0.0 and 1.0. The HSA fraction defaults to 1.0 (spouse inherits intact, per IRS rules). A 3-element list is accepted for backward compatibility and is automatically extended with 1.0 for HSA",
        "units": None,
        "notes": None,
    },
    "bequest": {
        "section": "solver_options",
        "type": "float",
        "description": 'Target bequest value in today\'s dollars (in units). Used when objective = "maxSpending".',
        "units": "usd",
        "notes": None,
    },
    "bigMaca": {
        "section": "solver_options",
        "type": "float",
        "description": '*(Advanced)* Big-M value for the upper bound on the above-400%-FPL ACA MAGI bracket (when withACA = "optimize"). Should exceed any plausible annual MAGI.',
        "units": None,
        "notes": None,
    },
    "bigMamo": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Big-M value for at-most-one (AMO) constraints (mutually exclusive operations). Should exceed any individual withdrawal, conversion, or surplus deposit.",
        "units": None,
        "notes": None,
    },
    "bigMltcg": {
        "section": "solver_options",
        "type": "float",
        "description": '*(Advanced)* Big-M value for LTCG bracket binary constraints (when withLTCG = "optimize"). Scaled by the inflation factor $\\gamma_n$ each year. Defaults to 3 × T20_n per year when omitted.',
        "units": "years",
        "notes": None,
    },
    "bigMniit": {
        "section": "solver_options",
        "type": "float",
        "description": '*(Advanced)* Big-M value for NIIT threshold binary constraints (when withNIIT = "optimize"). Scaled by the inflation factor $\\gamma_n$ each year. Defaults to 3 × T20_n per year when omitted.',
        "units": "years",
        "notes": None,
    },
    "bigMss": {
        "section": "solver_options",
        "type": "float",
        "description": '*(Advanced)* Big-M value for the Social Security taxability MIP formulation (when withSSTaxability = "optimize"). Should exceed provisional income and related quantities.',
        "units": None,
        "notes": None,
    },
    "block_size": {
        "section": 'for_method_=_"bootstrap_sor"',
        "type": "integer",
        "description": 'Block length for block/circular/stationary bootstraps. Ignored when bootstrap_type = "iid". Default is 1',
        "units": None,
        "notes": None,
    },
    "bootstrap_type": {
        "section": 'for_method_=_"bootstrap_sor"',
        "type": "string",
        "description": 'Type of bootstrap resampling. Valid values: "iid" (independent draws), "block", "circular", "stationary". Default is "iid"',
        "units": None,
        "notes": None,
    },
    "case_name": {
        "section": "root_level_parameters",
        "type": "string",
        "description": "Name of the case/plan",
        "units": None,
        "notes": None,
    },
    "constrain_mean": {
        "section": 'for_method_=_"histogaussian",_"histolognormal",_"historical_copula",_"garch_dcc",_"gmm",_or_"hmm"',
        "type": "boolean",
        "description": "*(Optional)* When true, shifts each generated series additively so its arithmetic mean matches the historical window arithmetic mean. Preserves distribution shape (variance, skew, autocorrelation); only the mean is corrected. Useful to eliminate sampling bias in short scenarios. Default is false",
        "units": None,
        "notes": None,
    },
    "correlations": {
        "section": 'for_method_=_"gaussian"_or_"lognormal"',
        "type": "list of 6 floats",
        "description": "Pearson correlation coefficient (range -1 to 1) for upper triangle: (1,2), (1,3), (1,4), (2,3), (2,4), (3,4). Standard representation in finance and statistics.",
        "units": None,
        "notes": None,
    },
    "crisis_weight": {
        "section": 'for_method_=_"bootstrap_sor"',
        "type": "float",
        "description": "*(TOML only)* Sampling multiplier applied to crisis years. Default is 1.0 (no overweighting)",
        "units": "years",
        "notes": None,
    },
    "crisis_years": {
        "section": 'for_method_=_"bootstrap_sor"',
        "type": "list of integers",
        "description": "*(TOML only)* Calendar years to overweight in sampling (e.g. [1929, 2008]). Default omitted = no overweighting",
        "units": "years",
        "notes": None,
    },
    "date_of_birth": {
        "section": "basic_info",
        "type": "list of N_i ISO dates",
        "description": 'Date of birth for each individual in ISO format (e.g., "1967-01-15"). Defaults to "1965-01-15" if not specified',
        "units": None,
        "notes": None,
    },
    "default_plots": {
        "section": "results",
        "type": "string",
        "description": 'Default plot display mode. Valid values: "nominal" (nominal dollars), "today" (today\'s dollars)',
        "units": "usd",
        "notes": None,
    },
    "description": {
        "section": "root_level_parameters",
        "type": "string",
        "description": "A short text describing the purpose of the case",
        "units": None,
        "notes": None,
    },
    "dividend_rate": {
        "section": "units_of_measure",
        "type": "float",
        "description": "Dividend rate as a percentage (e.g., 1.72 for 1.72%)",
        "units": "percent",
        "notes": None,
    },
    "epsilon": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Lexicographic weight added to the objective to break ties. Adds a linearly increasing penalty to Roth conversions (earlier years are cheaper) to frontload conversions, and when N_i = 2, also penalizes withdrawals of spouse 2 to favor withdrawals from spouse 1. Use a very small value so the primary objective dominates.",
        "units": "years",
        "notes": None,
    },
    "fixedSpending": {
        "section": "solver_options",
        "type": "float",
        "description": 'Pin first-year spending to a fixed value (in today\'s dollars, in units) for objective = "maxSpending". When set, the optimizer holds g[0] at this level and uses spendingSlack to vary spending dynamically across years. Useful for studying dynamic spending paths at a committed level (e.g., from the efficient frontier).',
        "units": "usd",
        "notes": None,
    },
    "from": {
        "section": 'for_method_=_"historical",_"historical_average",_"histogaussian",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_or_"hmm"',
        "type": "integer",
        "description": "Starting year for historical data range (must be between 1928 and 2025). Default is 1928",
        "units": "years",
        "notes": None,
    },
    "gap": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Relative MILP gap used by solvers and to scale convergence tolerances.",
        "units": None,
        "notes": None,
    },
    "generic": {
        "section": 'for_type_=_"spouses"_*(python_api_only)*',
        "type": "2D array",
        "description": 'A single [initial, final] allocation pair shared by both spouses and applied uniformly across all their accounts. Structure: [[s0, b0, t0, c0], [sf, bf, tf, cf]]. Simpler to configure than "individual" when both spouses follow the same investment strategy.',
        "units": None,
        "notes": None,
    },
    "heirs_rate_on_tax_deferred_estate": {
        "section": "units_of_measure",
        "type": "float",
        "description": "Tax rate (as percentage, e.g., 30.0 for 30%) that heirs will pay on inherited tax-deferred and HSA accounts. Non-spouse HSA beneficiaries must include the full inherited HSA balance as ordinary income (IRC §223(f)(8)(B))",
        "units": "percent",
        "notes": None,
    },
    "hsa": {
        "section": 'for_type_=_"account"',
        "type": "3D array",
        "description": "*(Optional)* Asset allocation bounds for HSA accounts (same structure as taxable). When omitted, HSA uses the same allocation as tax-free",
        "units": None,
        "notes": None,
    },
    "hsa_savings_balances": {
        "section": "savings_assets",
        "type": "list of N_i floats",
        "description": "*(Optional)* Initial balance in Health Savings Accounts (HSA) for each individual (in thousands of dollars). Defaults to [0.0] (or [0.0, 0.0] for married). HSA contributions must stop at Medicare enrollment (~age 65); see HSA ctrb column in the HFP file",
        "units": "thousands_usd",
        "notes": None,
    },
    "includeMedicarePartD": {
        "section": "solver_options",
        "type": "boolean",
        "description": "Whether to include Medicare Part D premiums (IRMAA surcharges use same MAGI brackets as Part B). Set to false if you have other drug coverage (e.g., employer, VA).",
        "units": None,
        "notes": None,
    },
    "init_regime": {
        "section": 'for_method_=_"hmm"',
        "type": "integer",
        "description": "*(Optional)* Index of the starting regime for sequence generation (0 to n_components − 1). Default omitted = start from the stationary distribution",
        "units": None,
        "notes": None,
    },
    "interpolation_center": {
        "section": "asset_allocation",
        "type": "float",
        "description": 'For "s-curve": the year (measured from the start of the plan) at which the transition is halfway complete — the inflection point of the sigmoid. Ignored for "linear".',
        "units": "years",
        "notes": None,
    },
    "interpolation_method": {
        "section": "asset_allocation",
        "type": "string",
        "description": 'Method for gliding the allocation from initial to final values over time. "linear" = straight-line transition (equal steps each year). "s-curve" = smooth sigmoid (slow change at first, fast in the middle, slow again at the end), controlled by interpolation_center and interpolation_width.',
        "units": "years",
        "notes": None,
    },
    "interpolation_width": {
        "section": "asset_allocation",
        "type": "float",
        "description": 'For "s-curve": controls the steepness of the transition. Smaller values produce a sharper change; larger values spread the change over more years (±width years around the center). Ignored for "linear".',
        "units": "years",
        "notes": None,
    },
    "life_expectancy": {
        "section": "basic_info",
        "type": "list of N_i integers",
        "description": "Life expectancy in years for each individual",
        "units": "years",
        "notes": None,
    },
    "maxIter": {
        "section": "solver_options",
        "type": "integer",
        "description": "*(Advanced)* Maximum number of iterations for the self-consistent loop. Must be at least 1.",
        "units": None,
        "notes": None,
    },
    "maxRothConversion": {
        "section": "solver_options",
        "type": "float or string",
        "description": 'Maximum annual Roth conversion amount (in units). Use "file" to take per-year limits from time lists; omit for no cap (except last year).',
        "units": "years",
        "notes": None,
    },
    "maxTime": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Per-iteration solver time limit in seconds.",
        "units": None,
        "notes": None,
    },
    "medicarePartDBasePremium": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Optional)* Monthly Part D base premium per person in today's dollars. Omit or set to 0 to model IRMAA only. National average is roughly \\$39–47/month.",
        "units": "usd",
        "notes": None,
    },
    "method": {
        "section": "units_of_measure",
        "type": "string",
        "description": 'Method for determining rates. Valid values: "trailing-30", "optimistic", "conservative", "user", "historical", "historical average", "gaussian", "histogaussian", "lognormal", "histolognormal", "bootstrap_sor", "var", "garch_dcc", "historical_copula", "gmm", "hmm", "dataframe"',
        "units": None,
        "notes": None,
    },
    "minTaxableBalance": {
        "section": "solver_options",
        "type": "array",
        "description": "Minimum taxable account balance per spouse (in today's units). Values are indexed for inflation. Constraints apply from year 2 through each individual's life horizon.",
        "units": "years",
        "notes": None,
    },
    "n_components": {
        "section": 'for_method_=_"hmm"',
        "type": "integer",
        "description": "*(Optional)* Number of hidden states (market regimes). Default is 3. Must be ≥ 2",
        "units": None,
        "notes": None,
    },
    "names": {
        "section": "basic_info",
        "type": "list of strings",
        "description": "Names of the individuals in the plan. Must contain 1 or 2 names. Length determines N_i",
        "units": None,
        "notes": None,
    },
    "netSpending": {
        "section": "solver_options",
        "type": "float",
        "description": 'Target net spending amount in today\'s dollars (in units). Used when objective = "maxBequest".',
        "units": "usd",
        "notes": None,
    },
    "noLateSurplus": {
        "section": "solver_options",
        "type": "boolean",
        "description": "Disallow surplus deposits in the final two years of the plan.",
        "units": "years",
        "notes": None,
    },
    "noRothConversions": {
        "section": "solver_options",
        "type": "string",
        "description": 'Name of individual for whom Roth conversions are disabled, or "none" to allow conversions for all.',
        "units": None,
        "notes": None,
    },
    "obbba_expiration_year": {
        "section": "units_of_measure",
        "type": "integer",
        "description": "Year when the OBBBA (One Big Beautiful Bill Act) provisions expire. Default is 2032",
        "units": "years",
        "notes": None,
    },
    "objective": {
        "section": "optimization_parameters",
        "type": "string",
        "description": 'Optimization objective. Valid values: "maxSpending", "maxBequest"',
        "units": None,
        "notes": None,
    },
    "oppCostX": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Opportunity cost applied to Roth conversions (percent).",
        "units": "percent",
        "notes": None,
    },
    "other_medical_expenses": {
        "section": "optimization_parameters",
        "type": "float",
        "description": "*(Optional)* Annual non-Medicare qualified medical expenses (dental, vision, co-pays, deductibles, etc.) in today's dollars ($k). HSA withdrawals are capped by Medicare + this amount each year. Pre-Medicare years: only this amount is eligible (Medicare costs are zero). Default 0.0. Corresponds to plan.setMedicalExpenses()",
        "units": "usd",
        "notes": None,
    },
    "pension_ages": {
        "section": "fixed_income",
        "type": "list of N_i floats",
        "description": "Age at which pension starts for each individual",
        "units": None,
        "notes": None,
    },
    "pension_indexed": {
        "section": "fixed_income",
        "type": "list of N_i booleans",
        "description": "Whether each pension is indexed for inflation",
        "units": None,
        "notes": None,
    },
    "pension_monthly_amounts": {
        "section": "fixed_income",
        "type": "list of N_i floats",
        "description": "Monthly pension amount for each individual (in dollars). Use 0 if no pension",
        "units": "usd",
        "notes": None,
    },
    "pension_survivor_fraction": {
        "section": "fixed_income",
        "type": "list of N_i floats",
        "description": "Fraction of pension (0–1) continuing to surviving spouse. 0 = single-life. Typical: 0, 0.5, 0.75, 1.0",
        "units": None,
        "notes": None,
    },
    "previousMAGIs": {
        "section": "solver_options",
        "type": "array",
        "description": "*(Advanced)* Two-element list of prior-year MAGI values (in units) for Medicare calculations.",
        "units": "years",
        "notes": None,
    },
    "rate_seed": {
        "section": 'for_method_=_"gaussian",_"histogaussian",_"lognormal",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_or_"hmm"',
        "type": "integer",
        "description": "Random seed for reproducible stochastic rates. Default omitted = seed chosen randomly",
        "units": None,
        "notes": None,
    },
    "reg_trans": {
        "section": 'for_method_=_"hmm"',
        "type": "float",
        "description": "*(Optional)* Additive Laplace smoothing applied to transition counts to prevent zero-probability transitions. Default is 0.001. Must be > 0",
        "units": None,
        "notes": None,
    },
    "relTol": {
        "section": "solver_options",
        "type": "float",
        "description": "*(Advanced)* Relative convergence tolerance for the self-consistent loop objective.",
        "units": None,
        "notes": None,
    },
    "reproducible_rates": {
        "section": 'for_method_=_"gaussian",_"histogaussian",_"lognormal",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_or_"hmm"',
        "type": "boolean",
        "description": "Whether stochastic rates should be reproducible. Default is false",
        "units": None,
        "notes": None,
    },
    "reverse_sequence": {
        "section": 'for_method_=_"historical",_"histogaussian",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_"hmm",_"gaussian",_or_"lognormal"_(varying_rates_only)',
        "type": "boolean",
        "description": "If true, reverse the rate sequence along the time axis (e.g. last year first). Default is false. Ignored for fixed/constant rate methods. Used for both single-scenario and Historical Range runs.",
        "units": "years",
        "notes": None,
    },
    "roll_sequence": {
        "section": 'for_method_=_"historical",_"histogaussian",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_"hmm",_"gaussian",_or_"lognormal"_(varying_rates_only)',
        "type": "integer",
        "description": "Number of years to roll (shift) the rate sequence; positive shifts toward the end, values wrap. Default is 0. Ignored for fixed/constant rate methods. Used for both single-scenario and Historical Range runs.",
        "units": "years",
        "notes": None,
    },
    "sexes": {
        "section": "basic_info",
        "type": "list of N_i strings",
        "description": '*(Optional)* Biological sex for each individual. Valid values: "M" or "F". Defaults to ["F"] for single and ["M","F"] for married when omitted. Recommended to set explicitly, especially when using longevity-risk sampling in stochastic spending',
        "units": None,
        "notes": None,
    },
    "shrink": {
        "section": 'for_method_=_"var"',
        "type": "boolean",
        "description": "*(Optional)* When true, applies spectral shrinkage to the VAR(1) coefficient matrix to improve stability and reduce estimation error, especially for short historical windows. Default is false",
        "units": None,
        "notes": None,
    },
    "slcsp_annual": {
        "section": "aca_settings",
        "type": "float",
        "description": "Annual benchmark Silver plan (SLCSP) premium in today's dollars (in units). Set to 0 to disable ACA. Inflated internally by the plan's inflation factor.",
        "units": "usd",
        "notes": None,
    },
    "smile_delay": {
        "section": 'conditional_parameters_for_spending_profile_=_"smile"',
        "type": "integer",
        "description": "Number of years from the start before spending begins to decrease (0 to plan duration - 2). Default is 0",
        "units": "years",
        "notes": None,
    },
    "smile_dip": {
        "section": 'conditional_parameters_for_spending_profile_=_"smile"',
        "type": "integer",
        "description": 'Percentage to decrease spending during the "slow-go" years (0-100). Default is 15',
        "units": "percent",
        "notes": None,
    },
    "smile_increase": {
        "section": 'conditional_parameters_for_spending_profile_=_"smile"',
        "type": "integer",
        "description": "Percentage to increase (or decrease if negative) spending over the time span (-100 to 100). Default is 12",
        "units": "percent",
        "notes": None,
    },
    "social_security_ages": {
        "section": "fixed_income",
        "type": "list of N_i floats",
        "description": "Age at which Social Security benefits start for each individual",
        "units": None,
        "notes": None,
    },
    "social_security_pia_amounts": {
        "section": "fixed_income",
        "type": "list of N_i integers",
        "description": "Primary Insurance Amount (PIA) for Social Security for each individual (in dollars)",
        "units": "usd",
        "notes": None,
    },
    "social_security_trim_pct": {
        "section": "fixed_income",
        "type": "integer",
        "description": "*(Optional)* Percentage reduction applied to Social Security benefits from social_security_trim_year onward. Range 0–100. Use to model trust-fund shortfall scenarios (e.g. 23). Omit or set to 0 for no reduction",
        "units": "percent",
        "notes": None,
    },
    "social_security_trim_year": {
        "section": "fixed_income",
        "type": "integer",
        "description": "*(Required when social_security_trim_pct > 0)* Calendar year when the SS benefit reduction begins. Default UI value is 2033 (SSA Trustees Report projection for OASI exhaustion). Must be supplied alongside social_security_trim_pct",
        "units": "years",
        "notes": None,
    },
    "solver": {
        "section": "solver_options",
        "type": "string",
        "description": 'Solver to use for optimization. Valid values: "default", "HiGHS", "MOSEK". "default" automatically selects MOSEK when available and licensed, otherwise falls back to HiGHS.',
        "units": None,
        "notes": None,
    },
    "spendingSlack": {
        "section": "solver_options",
        "type": "integer",
        "description": "Percentage allowed to deviate from the spending profile. Spending stays within ±slack% of the profile. (0–100)",
        "units": "percent",
        "notes": None,
    },
    "spending_profile": {
        "section": "optimization_parameters",
        "type": "string",
        "description": 'Type of spending profile. Valid values: "flat", "smile"',
        "units": None,
        "notes": None,
    },
    "spia_buy_years": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of integers",
        "description": "Calendar year of purchase for each SPIA. Use a year before the plan start for an already-purchased annuity (no premium is deducted from the plan).",
        "units": "years",
        "notes": None,
    },
    "spia_indexed": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of booleans",
        "description": "true if the SPIA is CPI-indexed (payments grow with inflation); false for a fixed nominal payout.",
        "units": None,
        "notes": None,
    },
    "spia_individuals": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of integers",
        "description": "Individual index for each SPIA: 0 = first individual, 1 = second.",
        "units": None,
        "notes": None,
    },
    "spia_monthly_incomes": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of floats",
        "description": "Monthly income in today's dollars for each SPIA. Payments begin in the purchase year and are fully taxable as ordinary income.",
        "units": "usd",
        "notes": None,
    },
    "spia_premiums": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of floats",
        "description": "Lump-sum purchase price in dollars for each SPIA. Deducted from the tax-deferred account in the buy year as a non-taxable IRA rollover. Set to 0 for annuities purchased before plan start.",
        "units": "usd",
        "notes": None,
    },
    "spia_survivor_fractions": {
        "section": "spia_(single_premium_immediate_annuity)",
        "type": "list of floats",
        "description": "Fraction of income (0–1) continuing to the surviving spouse after the annuitant's death. 0 = single-life annuity; 0.5, 0.75, or 1.0 = joint-and-survivor. Ignored for single individuals.",
        "units": None,
        "notes": None,
    },
    "spousal_surplus_deposit_fraction": {
        "section": "savings_assets",
        "type": "float",
        "description": "*(Married only)* Fraction of surplus to deposit in the second spouse's taxable account. Value between 0.0 and 1.0",
        "units": None,
        "notes": None,
    },
    "standard_deviations": {
        "section": 'for_method_=_"gaussian"_or_"lognormal"',
        "type": "list of 4 floats",
        "description": "Volatility in percent for each rate type (e.g., 17 for 17% annualized standard deviation)",
        "units": "percent",
        "notes": None,
    },
    "startRothConversions": {
        "section": "solver_options",
        "type": "integer",
        "description": "Year when Roth conversions can begin (clamped to the current year).",
        "units": "years",
        "notes": None,
    },
    "start_date": {
        "section": "basic_info",
        "type": "string",
        "description": 'Start date of the plan (e.g., "01-01", "01/01", "2026-01-01"). Only the month and day are used; the plan always starts in the current year. Defaults to "today" if not specified',
        "units": "years",
        "notes": None,
    },
    "state": {
        "section": "basic_info",
        "type": "string",
        "description": '*(Optional)* Two-letter US state abbreviation for state income tax calculations (e.g., "MN", "CA"). Omit or set to "" for federal-only (no state tax)',
        "units": None,
        "notes": None,
    },
    "status": {
        "section": "basic_info",
        "type": "string",
        "description": 'Filing status. Valid values: "single", "married"',
        "units": None,
        "notes": None,
    },
    "surviving_spouse_spending_percent": {
        "section": "optimization_parameters",
        "type": "integer",
        "description": "Percentage of spending amount for the surviving spouse (0-100). Default is 60",
        "units": "percent",
        "notes": None,
    },
    "swapRothConverters": {
        "section": "solver_options",
        "type": "integer",
        "description": "*(Advanced)* For plans involving spouses, only allow one spouse to perform Roth conversions per year. The year provided determines a transition year when roles are swapped. The sign selects who converts first: positive means person 1 can convert first and person 2 any time after; negative year means person 2 before and person 1 after. This option overrides the noRothConversions option.",
        "units": "years",
        "notes": None,
    },
    "tax-deferred": {
        "section": 'for_type_=_"account"',
        "type": "3D array",
        "description": "Asset allocation bounds for tax-deferred accounts (same structure as taxable)",
        "units": None,
        "notes": None,
    },
    "tax-free": {
        "section": 'for_type_=_"account"',
        "type": "3D array",
        "description": "Asset allocation bounds for tax-free accounts (same structure as taxable)",
        "units": None,
        "notes": None,
    },
    "tax_deferred_savings_balances": {
        "section": "savings_assets",
        "type": "list of N_i floats",
        "description": "Initial balance in tax-deferred accounts (e.g., 401k, traditional IRA) for each individual (in thousands of dollars)",
        "units": "thousands_usd",
        "notes": None,
    },
    "tax_free_savings_balances": {
        "section": "savings_assets",
        "type": "list of N_i floats",
        "description": "Initial balance in tax-free accounts (e.g., Roth IRA, Roth 401k) for each individual (in thousands of dollars)",
        "units": "thousands_usd",
        "notes": None,
    },
    "taxable": {
        "section": 'for_type_=_"account"',
        "type": "3D array",
        "description": "Asset allocation bounds for taxable accounts. Structure: [[[initial_stocks, initial_bonds, initial_tnotes, initial_cash], [final_stocks, final_bonds, final_tnotes, final_cash]]] for each individual. The four classes are: S&P 500 (stocks), Corporate Baa bonds, 10-year Treasury notes, and cash/inflation",
        "units": "years",
        "notes": None,
    },
    "taxable_cost_basis": {
        "section": "savings_assets",
        "type": "list of N_i floats",
        "description": "*(Optional)* Adjusted cost basis of each individual's taxable account (in thousands of dollars). A positive entry activates average-cost gain tracking for that person; a zero entry means *use the legacy approximation for that person* (current-year price appreciation only — not a zero-dollar basis). This allows partial knowledge: set the known basis for one spouse and 0 for the other. Omit the field entirely to use the legacy approximation for everyone",
        "units": "thousands_usd",
        "notes": None,
    },
    "taxable_savings_balances": {
        "section": "savings_assets",
        "type": "list of N_i floats",
        "description": "Initial balance in taxable accounts for each individual (in thousands of dollars)",
        "units": "thousands_usd",
        "notes": None,
    },
    "timePreference": {
        "section": "solver_options",
        "type": "float",
        "description": 'Subjective time preference rate (%/year). Values above 0 discount future spending exponentially, shifting the optimal spending profile earlier. Supported for "maxSpending". Has no effect when objective = "maxBequest".',
        "units": "years",
        "notes": None,
    },
    "to": {
        "section": 'for_method_=_"historical",_"historical_average",_"histogaussian",_"histolognormal",_"bootstrap_sor",_"var",_"garch_dcc",_"historical_copula",_"gmm",_or_"hmm"',
        "type": "integer",
        "description": "Ending year for historical data range (must be between 1928 and 2025, and greater than from). Default is 2025. garch_dcc requires at least 15 years of data (to - from ≥ 15)",
        "units": "years",
        "notes": None,
    },
    "type": {
        "section": "asset_allocation",
        "type": "string",
        "description": 'How the allocation is defined. "individual" — each person has their own set of ratios applied identically to all their accounts. "account" — separate ratios for each account type (taxable, tax-deferred, tax-free, HSA), allowing more aggressive allocation in tax-free accounts. "spouses" — a single shared set of ratios applied across all accounts and both spouses simultaneously, reducing the number of parameters needed. *(Note: "spouses" is only available via the Python API, not the Streamlit UI.)*',
        "units": None,
        "notes": None,
    },
    "units": {
        "section": "solver_options",
        "type": "string",
        "description": 'Units for amounts. Valid values: "1" (dollars), "k" (thousands), "M" (millions).',
        "units": "thousands_usd",
        "notes": None,
    },
    "values": {
        "section": 'for_method_=_"user",_"gaussian",_or_"lognormal"',
        "type": "list of 4 floats",
        "description": "Mean returns in percent: [S&P 500, Corporate Baa bonds, 10-year Treasury notes, Inflation] (e.g., 7 for 7%)",
        "units": "percent",
        "notes": None,
    },
    "verbose": {
        "section": "solver_options",
        "type": "boolean",
        "description": "When true, prints detailed solver iteration logs to the console. Supported by HiGHS and MOSEK; output format varies by solver. Useful for diagnosing infeasibility or slow convergence.",
        "units": None,
        "notes": None,
    },
    "withACA": {
        "section": "solver_options",
        "type": "string",
        "description": 'ACA marketplace premium handling (when slcsp_annual > 0). "loop" (default): compute ACA cost in SC loop each iteration using the exact piecewise-linear ACA formula. "optimize": co-optimize ACA bracket selection within the LP — enables the optimizer to shift MAGI across brackets for better plan objectives (expert; can be slower; applies 2026 rules only).',
        "units": None,
        "notes": None,
    },
    "withDecomposition": {
        "section": "solver_options",
        "type": "string",
        "description": '*(Advanced)* MIP decomposition strategy for plans with multiple "optimize" flags active simultaneously. "none" (default): monolithic MIP. "sequential": relax-and-fix heuristic — solves an LP relaxation, rounds all bracket-selector families (zm, za, zs, zl, zj) simultaneously, then solves a single MIP with those brackets fixed and zx free; fast but not globally optimal. "benders": classical Benders decomposition — separates bracket selection (master: zm, za, zs, zl, zj) from continuous planning (subproblem: continuous variables + zx); certifies global optimality within gap tolerance via accumulated dual cuts; typically converges in 1–3 iterations. Silently ignored when no bracket-selection binaries are present. HiGHS and MOSEK supported.',
        "units": None,
        "notes": None,
    },
    "withLTCG": {
        "section": "solver_options",
        "type": "string",
        "description": 'Long-term capital gains (LTCG) bracket handling. "loop" (default): ordinary income stacking computed in SC loop. "optimize": exact MILP formulation for LTCG bracket selection — binary variables determine which 0%/15%/20% bracket applies each year (expert; adds zl binary family).',
        "units": "years",
        "notes": None,
    },
    "withMedicare": {
        "section": "solver_options",
        "type": "string",
        "description": 'Medicare Part B and Part D IRMAA handling. Valid values: "none", "loop", "optimize" (expert). When not "none", Part B and Part D premiums (including IRMAA) are included; Part D can be disabled or given a base premium via the options below.',
        "units": None,
        "notes": None,
    },
    "withNIIT": {
        "section": "solver_options",
        "type": "string",
        "description": 'Net Investment Income Tax (NIIT) handling. "loop" (default): NIIT computed after each SC iteration. "optimize": exact MILP formulation — binary variable determines whether MAGI exceeds the NIIT threshold (\\$200k single / \\$250k MFJ) each year (expert; adds zj binary family; most effective when withLTCG = "optimize" is also set).',
        "units": "years",
        "notes": None,
    },
    "withSCLoop": {
        "section": "solver_options",
        "type": "boolean",
        "description": "Whether to run the self-consistent loop to full convergence. When false, the solver always performs exactly two iterations: the first establishes ordinary income (G_n) so that LTCG bracket room is computed correctly; the second is the accepted solve. Medicare IRMAA and SS taxability are not converged in this mode — Medicare premiums are computed for display only. Useful for speed in Monte Carlo runs where full convergence is not required.",
        "units": None,
        "notes": None,
    },
    "withSSAges": {
        "section": "solver_options",
        "type": "string or list",
        "description": 'Social Security claiming-age optimization. "fixed" (default): use the claiming ages provided via setSocialSecurity() or entered on the Fixed Income page. "optimize": let the MIP optimizer choose the optimal claiming month (any month from age 62 to 70, i.e. 97 choices) for all individuals. An individual name (e.g. "Jack") or a list of names (e.g. ["Jack"], ["Jack", "Jill"]) restricts optimization to those individuals — useful when one spouse has already claimed and only the other\'s age should be optimized. The optimizer selects own benefits directly via LP; spousal and survivor benefit offsets are updated each SC iteration. Individuals whose current age already exceeds their recorded claiming age are always treated as fixed regardless of this setting. After solving, plan.ssecAges contains the (possibly updated) claiming ages.',
        "units": None,
        "notes": None,
    },
    "withSSTaxability": {
        "section": "solver_options",
        "type": "string or float",
        "description": 'Controls how the taxable fraction of Social Security benefits (Ψ) is determined. The IRS provisional income (PI) formula taxes 0% of SS below \\$25k/\\$32k PI (single/MFJ), up to 50% between those and \\$34k/\\$44k, and up to 85% above. "loop" — recomputes Ψ each SC-loop iteration based on that year\'s projected income (recommended). "optimize" — encodes the IRS piecewise formula exactly as a MIP with binary variables (expert; can require a larger gap). Float in [0, 0.85] — pins Ψ to a fixed value: 0.0 (income well below the lower threshold), 0.5 (income in the mid range), or 0.85 (income above the upper threshold).',
        "units": "years",
        "notes": None,
    },
    "worksheet_hide_zero_columns": {
        "section": "results",
        "type": "boolean",
        "description": "When true (default false), the Streamlit **Worksheets** page omits numeric columns where every value is zero (within a small tolerance). The year and age columns are never removed. Applies to the **on-screen display only** — the saved Excel workbook always retains all columns.",
        "units": "years",
        "notes": None,
    },
    "worksheet_real_dollars": {
        "section": "results",
        "type": "boolean",
        "description": "When true (default false), all currency values in both the on-screen tables and the saved Excel workbook are divided by the cumulative inflation factor gamma_n, converting nominal dollars to today's (real) dollars. The saved Excel filename gains a _real suffix to distinguish it from the nominal version.",
        "units": "usd",
        "notes": None,
    },
    "worksheet_show_ages": {
        "section": "results",
        "type": "boolean",
        "description": "When true (default false), adds per-person **age** columns next to year in both the on-screen tables and the saved Excel workbook. Each value is the individual's integer age on **December 31** of that row's calendar year. The cell is blank for years after that person's plan horizon.",
        "units": "years",
        "notes": None,
    },
}
